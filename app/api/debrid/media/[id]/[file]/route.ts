import {AllDebridError} from '../../../../../lib/alldebrid';
export async function GET(_request:Request,{params}:{params:Promise<{id:string;file:string}>}){try{const{id,file}=await params;if(!/^\d+$/.test(id)||!/^\d+$/.test(file))throw new AllDebridError('Invalid media selection.','INVALID_MEDIA',400);const base=process.env.KHEYFLIX_TRANSCODER_URL||'http://127.0.0.1:3101',response=await fetch(`${base}/probe/${id}/${file}`,{cache:'no-store'});if(!response.ok)return Response.json({error:{message:'Media information is unavailable.'}},{status:response.status});return new Response(response.body,{headers:{'Content-Type':'application/json','Cache-Control':'private, max-age=1800'}})}catch(error){const known=error instanceof AllDebridError?error:new AllDebridError('Media information is unavailable.');return Response.json({error:{code:known.code,message:known.message}},{status:known.status})}}
